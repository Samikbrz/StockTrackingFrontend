import { BarcodePipePipe } from './barcode-pipe.pipe';

describe('BarcodePipePipe', () => {
  it('create an instance', () => {
    const pipe = new BarcodePipePipe();
    expect(pipe).toBeTruthy();
  });
});
